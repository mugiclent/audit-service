# Environment variables — never commit real env files
.env
.env.local
.env.*.local

# Local Docker override — contains port bindings and env_file; never commit
docker-compose.override.yml

# Local secrets — filled-in copy of actions.env, never commit
actions.env

# Build output
dist/

# Dependencies
node_modules/

# Coverage
coverage/

# OS
.DS_Store
